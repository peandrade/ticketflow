# TicketFlow - Exemplo de variáveis de ambiente
# Copie este arquivo para .env.local e preencha os valores reais.
# Nunca versione .env.local com segredos reais.

NODE_ENV=development

# Banco de dados (PostgreSQL)
# Ex.: Neon/Supabase/Railway
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/ticketflow?schema=public"

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_xxx"
STRIPE_SECRET_KEY="sk_test_xxx"
STRIPE_WEBHOOK_SECRET="whsec_xxx"

# (Opcional) URL pública da app
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# (Opcional) Segredo de autenticação se usar next-auth/algum provider
# AUTH_SECRET="your-strong-secret"
